# Install httpd 


**Mode:** Ansible Developer

**Context:**

We want to install the httpd server on a group of webservers.

**Prompt:**

```
Add a task in the current playbook that installs the httpd package
```

**Result:**

Bob added a new task in the `playbook.yaml` file, installing the httpd package.

**Follow-up Actions:**

- Copy the httpd configuration template

---
